{
  "name": "xamora-studio",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "description": "Extensible visual XAML and HTML design studio",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
